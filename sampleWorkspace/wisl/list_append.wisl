predicate list(+x, alpha) {
    (x == null) * (alpha == nil);
    (x -> #v, #z) * list(#z, #beta) *
    (alpha == #v::#beta)
}

{ (x == #x) * (v == #v) * list(#x, #alpha) }
function append(x, v) {
  if (x = null) {
    x := new(3);
    [x] := v;
    [x + 1] := null;
    [x + 2] := 4;
    y := new(4);
    [y] := 44;
    w := y + 1;
    z := 22
  } else {
    t := [x + 1];
    t := append(t, v);
    [x + 1] := t
  };
  return x
}
{ list(ret, #alpha @ [#v]) }